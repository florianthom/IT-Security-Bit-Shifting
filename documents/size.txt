proc reduce(ref BigInt a) {
 for nat i:= a.size-1 downto 0 while a[i]=0 {}
 // i ist der Index der höchsten Zelle
 a.spart= i+1;
}

expand(ref BigInt, nat) verlängert den signifikanten Teil des Arrays
auf newSize Zellen und setzt die neuen signifikanten Zellen auf 0.

 Cell value[size]; // cell array

 size ist die Größe des Zellenfeldes, Indices: 0...size-1